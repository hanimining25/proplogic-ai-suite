
import React from 'react';
import { Card, CardContent, CardHeader, CardTitle, CardDescription } from '@/components/ui/card';

const TPBuilder = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>TP Builder</CardTitle>
        <CardDescription>Technical Proposal Builder.</CardDescription>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">Content for TP Builder goes here. This feature is under construction.</p>
      </CardContent>
    </Card>
  );
};

export default TPBuilder;
