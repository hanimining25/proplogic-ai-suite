
import React from 'react';
import { Card, CardContent, CardHeader, CardTitle, CardDescription } from '@/components/ui/card';

const RFPDraftBuilder = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>RFP Draft Builder</CardTitle>
        <CardDescription>Build drafts for RFP responses.</CardDescription>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">Content for RFP Draft Builder goes here. This feature is under construction.</p>
      </CardContent>
    </Card>
  );
};

export default RFPDraftBuilder;
