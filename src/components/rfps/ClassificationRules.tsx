
import React from 'react';
import { Card, CardContent, CardHeader, CardTitle, CardDescription } from '@/components/ui/card';

const ClassificationRules = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Classification Rules</CardTitle>
        <CardDescription>Manage classification rules for RFPs.</CardDescription>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">Content for Classification Rules goes here. This feature is under construction.</p>
      </CardContent>
    </Card>
  );
};

export default ClassificationRules;
